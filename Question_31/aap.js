"use strict";
// Question 31
let userNames = ['admin', 'shahid afridi', 'babar', 'amir', 'sarfaraz'];
if (userNames.length === 0) {
    console.log('We need to find some users');
}
else {
    userNames = [];
    console.log('All user names have been removed. ' + userNames.length);
}
